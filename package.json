{
  "name": "lr-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "ios": "npm run build && export DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer && npx cap sync ios && xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Release -destination id=$IPHONE -derivedDataPath tmp/ios-dev -allowProvisioningUpdates -quiet build && xcrun devicectl device install app --device $IPHONE tmp/ios-dev/Build/Products/Release-iphoneos/App.app"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@capacitor-community/text-to-speech": "^8.0.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2",
    "@huggingface/transformers": "^4.3.0",
    "fflate": "^0.8.3",
    "idb-keyval": "^6.3.0",
    "onnxruntime-web": "^1.31.0-dev.20260914-8d85527a0",
    "pdfjs-dist": "^6.3.289"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^5.0.1"
  }
}
